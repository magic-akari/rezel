schema_version = 1

[component]
name = "rezel-json"
source_root = "languages/json/src"

[upstream]
repository = "https://github.com/lezer-parser/json"
commit = "a48f95d0d403b8b13f14d466216bf017dacd7a34"

[[snapshot]]
source = "src/highlight.js"
path = "alignment/upstream/json/highlight.json"

[[scope]]
rust_file = "generated.rs"
classification = "rezel-owned"
invariant = "The unchanged pinned JSON grammar is compiled into deterministic static tables accepted by the Rezel LR runtime."
representation = "Checked-in Rust data is generated by Rezel's Rust backend; the vendored grammar and its upstream CST fixtures preserve the language-level provenance."
evidence_files = [
	"languages/json/src/json.grammar",
	"tools/codegen/src/main.rs",
	"languages/json/tests/parse.rs",
]

[[scope]]
rust_file = "highlighting.rs"
classification = "adapted"
upstream_file = "src/highlight.js"
upstream_symbols = ["jsonHighlighting"]
invariant = "JSON node selectors map to the same official syntactic tags as the pinned language package."
representation = "A feature-gated Rust NodePropSource replaces the JavaScript styleTags object."
evidence_files = ["languages/json/tests/highlighting.rs"]

[[scope]]
rust_file = "lib.rs"
classification = "rezel-owned"
invariant = "Downstream users receive one cheaply cloned recovering JSON parser, zero-copy typed CST wrappers, and optional syntactic highlighting."
representation = "A Rust crate facade re-exports the generated parser, terms, and typed syntax while a feature gate selects JSON highlighting."
evidence_files = [
	"languages/json/tests/contract.rs",
	"languages/json/tests/parse.rs",
	"languages/json/tests/typed.rs",
]

[[scope]]
rust_file = "terms.rs"
classification = "rezel-owned"
invariant = "Named JSON grammar terms are exposed as deterministic Rust constants with the generated parser's term identifiers."
representation = "Rezel's Rust backend emits constants from the unchanged pinned grammar."
evidence_files = ["tools/codegen/src/main.rs"]

[[scope]]
rust_file = "typed.rs"
classification = "rezel-owned"
invariant = "The complete JSON typed schema exposes zero-copy, identity-safe wrappers and lazy direct-child accessors over the generic CST."
representation = "Checked generated Rust implements Rezel's TypedNode API from src/json.typed.toml without constructing a semantic JSON model."
evidence_files = [
	"languages/json/src/json.typed.toml",
	"tools/codegen/src/main.rs",
	"languages/json/tests/typed.rs",
]
