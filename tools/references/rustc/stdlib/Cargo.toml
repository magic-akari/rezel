[package]
name = "rezel-rust-stdlib-reference"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
repository.workspace = true
description = "Rust standard-library source verifier for Rezel"

[dependencies]
rezel-lang-rust = { path = "../../../../languages/rust" }

[lints]
workspace = true
